# Reversa — Project Configuration
# This file is managed by the installer. For customizations, edit config.user.toml

[project]
name = ""
version = "1.0.0"

[user]
name = ""
chat_language = "pt-br"
doc_language = "pt-br"

[output]
folder = "_reversa_sdd"

[agents]
installed = [
  "reversa-scout",
  "reversa-archaeologist",
  "reversa-detective",
  "reversa-architect",
  "reversa-writer",
  "reversa-reviewer",
  "reversa-visor",
  "reversa-data-master",
  "reversa-design-system",
  "reversa-extract-soul"
]

[engines]
installed = []

[analysis]
answer_mode = "chat"
doc_level = "completo"

# Organization of specs generated by agents.
# This section is populated by Reversa after the Scout suggests a structure
# and the user confirms the choice in the menu (right after the doc_level question).
# To re-display the menu, remove the entire [specs] section and run Reversa again.
[specs]
layout = ""              # constant "feature-folder" after the first run
granularity = ""         # module | use-case | endpoint | hybrid | feature | custom
custom_folders = []      # populated only when granularity == "custom"
scout_suggestion = ""    # record of the Scout's original suggestion, immutable after the 1st run
decided_at = ""          # ISO 8601 timestamp of the user's choice
